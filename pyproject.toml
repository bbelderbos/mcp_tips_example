[project]
name = "tips-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp>=1.27.0",
    "python-decouple>=3.8",
    "slack-bolt>=1.28.0",
]
